
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlops-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mlops
  template:
    metadata:
      labels:
        app: mlops
    spec:
      containers:
      - name: mlops-container
        image: anantutkarshiitj/mlops_major:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000
